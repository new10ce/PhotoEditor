import { ProfessionalPhotoUser } from './professional-photo-user';

describe('ProfessionalPhotoUser', () => {
  it('should create an instance', () => {
    expect(new ProfessionalPhotoUser()).toBeTruthy();
  });
});
